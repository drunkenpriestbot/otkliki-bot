"""
Для каждого кандидата от monitor_telegram.py (прошедшего classify.py) просит
Claude (через claude CLI, на Pro-подписке) оценить релевантность и, если заказ
подходит, написать черновик персонализированного отклика. Не подходящие заказы
отбрасываются здесь же (второй, более надёжный фильтр после Groq).

BRIEF и PORTFOLIO ниже — ПУСТЫЕ ШАБЛОНЫ, заполни своими реальными данными.
SALES_PRINCIPLES и WRITING_RULES — рабочая методика написания отклика
(см. писание-отклика-skill.md рядом, то же самое отдельным файлом) — оставлены
как есть, это универсальная структура, не привязанная к конкретной нише.

См. LESSONS_LEARNED.md, пункт 3 — почему вызовы claude -p сделаны именно так
(чистый tmp cwd, --no-session-persistence, nonce, пауза между вызовами).
"""
import json
import subprocess
import sys
import tempfile
import time
import uuid

BRIEF = """
Контекст обо мне (используй при написании отклика, не упоминай это как "бриф"):
- Монтажёр вертикальных видео (Reels/Shorts/TikTok), в монтаже видео более 3 лет.
- Моё УТП: монтирую как простые ролики, так и сложные проекты с моушн-графикой,
  3D-трекингом и продвинутым саунд-дизайном — могу закрыть и лёгкую нарезку, и
  технически сложный проект под один прайс-диапазон, не отказываюсь от объёма.
  Уверенно использую нейросети (генерация видео/фото, апскейл) там, где это
  ускоряет или улучшает результат.
- Работаю в Premiere Pro и After Effects — НЕ упоминай CapCut клиенту никогда
  (использую его внутренне для отдельных фич, но Premiere/AE ценятся выше,
  упоминание CapCut снижает доверие).
- Подписываюсь в конце откликов как "Артём".
- Реальные кейсы по просмотрам на роликах: 401К, 287К, 883К, 910К — вставлять
  РЕДКО, только когда органично ложится в одну фразу (например, в блоке про
  опыт/надёжность), не как стандартную приписку к каждому отклику.
- НИКОГДА не предлагай и не упоминай готовность делать тестовое/пробное
  задание — даже если заказчик прямо просит тестовое, просто не затрагивай эту
  тему в отклике (мягко пропусти, не отказывайся явно и не соглашайся).
- Если заказчик в самом тексте вакансии явно спрашивает точную цену/прайс —
  не игнорируй это шаблоном вслепую: упомяни короткую фразу, что цена
  обсуждается индивидуально после уточнения деталей/объёма работы (не называй
  конкретную цифру и не выдумывай прайс).
"""

PORTFOLIO = """
Мои реальные работы (название — тема/ниша — короткая деталь — ссылка).

Внутренние "стили" для подбора примера (НЕ произносить клиенту как термин,
см. правило 3 в SALES_PRINCIPLES):
- СЛОЖНЫЙ МОНТАЖ/МОУШН — много моушн-графики, 3D, продвинутый саунд-дизайн.
- ГОВОРЯЩАЯ ГОЛОВА/ИНФОГРАФИКА — спикер в кадре + анимированные текст/вставки.
- СТОРИТЕЛЛИНГ — личная история/мотивация + динамичные субтитры.
- ЭДИТ/ПРОМО — рекламная подача без живого спикера, акцент на визуальный приём.
- AI-ГЕНЕРАЦИЯ — генерация видео/фото нейросетями как часть монтажа.

СЛОЖНЫЙ МОНТАЖ/МОУШН (для клиентов, которым нужна команда/сложный монтаж/
моушн-графика — самые продвинутые работы, но не первый выбор для простого
заказа):
- Промо для фриланс-сообщества — моушн-графика, стилизованная 3D, неоновые
  эффекты, динамичная камера, самый продвинутый саунд-дизайн — https://t.me/krayne_portfolio/3/22
- Чистая моушн-графика в стиле Apple, демонстрация функций приложения,
  плавные переходы, светящиеся интерактивные элементы — https://t.me/krayne_portfolio/9/21

ГОВОРЯЩАЯ ГОЛОВА/ИНФОГРАФИКА (e-commerce, инфобиз, эксперты в кадре):
- Видео про важность дизайна карточек маркетплейсов — динамичные текстовые
  наложения с глитч-эффектом и сменой цвета, анимированные вставки карточек
  товаров — https://t.me/krayne_portfolio/3/23

СТОРИТЕЛЛИНГ:
- Личная история про онлайн-доход + AI-генерация модельных фото для одежды —
  быстрые переходы, яркие текстовые наложения, демонстрация AI-интерфейса,
  крутые анимированные субтитры — https://t.me/krayne_portfolio/3/24

ЭДИТ/ПРОМО (демонстрация технических навыков):
- Эдит с Rolls-Royce — бесконечный зум, работа с масками и 3D-трекингом,
  премиальная подача — https://t.me/krayne_portfolio/3/25
- Промо сыворотки для лица — замедленная съёмка падения продукта в воду,
  анимация текста с ингредиентами, премиальный вид — https://t.me/krayne_portfolio/11/27

AI-ГЕНЕРАЦИЯ:
- Комедийная демонстрация генерации AI-изображений — живые кадры экрана +
  AI-пейзажи, текстовые надписи, саунд-дизайн под комедийный тайминг —
  https://t.me/krayne_portfolio/11/20

Полное портфолио (общая ссылка, если нет точного совпадения под конкретный
формат заказа): @Krayne_portfolio
Результаты клиентов: https://t.me/krayne_portfolio/7
Отзывы: https://t.me/krayne_portfolio/5

ПРАВИЛО ВЫБОРА ПРИМЕРА (это часть самого процесса написания отклика, а не
отдельный механический подбор ссылки):
1. Сначала пойми, под что заточен заказ (тема, ниша, требуемый стиль/настроение),
   и из этого выбирай — а не подбирай текст под случайно выбранную ссылку.
2. Если есть точное совпадение по теме/нише — используй его как "вот похожая
   работа".
3. Если точного нет, но есть близкое по стилю или сложности — можно использовать,
   но честно (например: "делал в похожем формате для другой категории").
4. Если вообще ничего близкого нет — НЕ вставляй пример и не притягивай его за
   уши, это выглядит хуже, чем отсутствие примера. Открытый вопрос и готовность
   взяться работают в этом случае сами по себе.
5. Если упоминаешь работу — вставляй ссылку органично в текст (не отдельным
   блоком и не как список), как будто прикладываешь её на лету в разговоре.
6. Если описываешь стиль словами — используй только конкретные детали (что
   видно на примере), не профессиональный жаргон/термин стиля.
"""

SALES_PRINCIPLES = """
Главные правила продаж, применимо к холодному текстовому отклику — не к звонку
и не к переговорам, а именно к первому сообщению:

1. НЕ ВПАРИВАЙ. Не лебези, не выпрашивай заказ, не пиши в стиле "очень хочу
   взяться", "буду рад любому заказу" — это позиция нужды, она снижает
   доверие. Тон — уверенный человек, у которого и так есть заказы, а не
   проситель.
2. ПОРТФОЛИО — ЭТО SOCIAL PROOF, не нужно его "продавать" словами. Просто
   показывай ссылку и короткий факт по делу — без восторженных эпитетов
   вроде "потрясающий результат", "невероятный эффект". Сухая уверенная
   подача убеждает сильнее, чем рекламные прилагательные.
3. ГОВОРИ НА ЯЗЫКЕ КЛИЕНТА, НЕ ЖАРГОНОМ. Внутренние термины/названия стилей
   — твой внутренний инструмент подбора примера из PORTFOLIO, их не нужно
   произносить клиенту как профессиональный термин. Если нужно словами
   описать стиль — описывай простыми словами (через конкретную деталь), а не
   названием стиля.
4. СЕРВИС = СНИМАЕШЬ С КЛИЕНТА ГОЛОВНУЮ БОЛЬ. Покажи, что уже понял суть
   задачи и готов взяться сразу, без лишних уточнений и без того, чтобы
   клиент сам разжёвывал то, что и так ясно из описания заказа.
5. НИКАКИХ АБСОЛЮТНЫХ ОБЕЩАНИЙ РЕЗУЛЬТАТА, который не зависит только от
   тебя (рост продаж, конверсии и т.п.) — обещай только то, что реально
   контролируешь (визуал/качество, сроки, стиль).
6. ЗАКРЫВАЙ БЕЗ ЗАВИСАНИЯ. Не тяни, не добавляй лишний открытый вопрос
   "просто чтобы завязать диалог" — заканчивай уверенно (закрывающая
   строка из СКЕЛЕТА), вопрос добавляется отдельно только если без него
   реально невозможно понять задачу, и не заменяет блоки скелета.
7. НЕПРЕДВЗЯТОСТЬ. Один и тот же уверенный тон и структура — независимо
   от бюджета заказа (маленький заказ не повод писать более жалобно,
   крупный — не повод писать более льстиво).
8. БЕЗ ЖАРГОНА И БЕЗ ШТАМПОВ КОПИРАЙТЕРА ("уникальный подход",
   "индивидуальный дизайн под ваш бренд" и т.п.) — конкретика вместо
   общих фраз.
"""

WRITING_RULES = """
Ниже — анонимизированный пример (не текст для копирования, а иллюстрация
структуры) реального отклика, который получал ответы на заказы. Это эталон
структуры. Не "вдохновляйся в общих чертах" — собирай отклик из тех же блоков,
в том же порядке, просто меняя содержание под текущий заказ и реальные данные
из BRIEF/PORTFOLIO выше.

ПРИМЕР (есть точное совпадение в портфолио):
"Здравствуйте! Я совсем недавно делал проект в нише, которая Вам требуется -
[конкретика], прикрепил работы ниже:
[Работа 1](ссылка на работу 1)
[Работа 2](ссылка на работу 2)
Работаю с [инструменты/методы из BRIEF] и на высоком уровне владею
[технология/навык из BRIEF], что помогает кратно улучшать результат. Если
спишемся в ближайшее время, готов предоставить первый результат уже завтра:)
В этой нише я уже [срок из BRIEF], это моё основное направление, и я был бы
рад долгосрочному сотрудничеству с Вами.
Надеюсь на Ваш ответ, с уважением, [Имя]!"

ПРИМЕР (точного совпадения нет, только смежные ниши):
"[Имя клиента], здравствуйте! Прикрепил пару своих последних работ в смежных
нишах:
[Работа 1](ссылка на работу 1)
[Работа 2](ссылка на работу 2)
Уже не раз работал с крупными объёмами заказов, и я был бы рад выполнять Ваши
задачи качественно и в кратчайшие сроки. Я работаю в совершенно разных
стилях и смогу быстро адаптировать [результат] под новые требования. Также
на высоком уровне владею [технология/навык из BRIEF].
Если мы спишемся в ближайшее время, готов предоставить первые результаты уже
сегодня:)
Надеюсь на Ваш ответ, с уважением, [Имя]!"

Оба примера — ОДНА И ТА ЖЕ структура (разница только в формулировке первого
блока: "делал проект именно в этой нише" против "смежные ниши"). Ни в одном
из примеров НЕТ открытого вопроса клиенту — оба сразу переходят к портфолио
и закрывают уверенной подачей, без выпрашивания уточнений. Используй эту же
структуру всегда, вопрос — только если без него реально невозможно понять,
что делать (и даже тогда вопрос не заменяет блоки ниже, а добавляется отдельно).

ВАЖНО — это структура, а не текст для копирования. Бери из примеров порядок
блоков и их смысл, но КАЖДОЕ предложение пиши заново своими словами под
конкретику текущего заказа и реальные данные из BRIEF/PORTFOLIO. Признак того,
что ты делаешь это неправильно: если твой черновик совпадает с примерами
выше более чем на пару слов в каждом предложении подряд — перепиши.
Обязательно используй детали из самого заказа (title/description), а не общую
фразу "в смежных нишах" без уточнения, о какой нише речь.

ПЕРЕФРАЗИРУЙ, НЕ КОПИРУЙ СЛОВА ЗАКАЗЧИКА. Если в тексте вакансии заказчик сам
описал задачу своими словами (например "монтировать динамичные и вовлекающие
reels"), не повторяй эти же слова в блоке про навыки почти дословно — это
выглядит как простое эхо, а не как реальная компетенция. Возьми суть того, что
нужно, и сформулируй её своим языком, через конкретику из BRIEF.

ЗАПРЕЩЁННЫЕ ФРАЗЫ (звучат как шаблонный текст нейросети, без смысла — НЕ
использовать ни в каком виде):
- "продумываю ритм/музыку/шрифты под площадку" — ритм/музыку нельзя
  "продумать" как отдельное абстрактное действие, это не звучит как факт о
  работе, а как пустая фраза. Если хочешь сказать что-то похожее — говори
  конкретно через результат (визуальные сцены, колористика, типографика), не
  через абстрактный глагол "продумываю".
- "не делаю шаблонные нарезки" — нарезка не может быть "шаблонной" или
  "нешаблонной", это бессмысленное противопоставление, сразу выдаёт
  нейротекст.
- любые другие абстракции без опоры на конкретное ремесло монтажа.

СКЕЛЕТ (следуй буквально, блок за блоком):
1. Приветствие (с именем клиента, если оно есть в заказе, как "Сергей,
   здравствуйте!" — если имени нет, просто "Здравствуйте!") + ОДНА фраза-
   переход к портфолио:
   - если есть точное/близкое совпадение по теме — аналог "Я совсем
     недавно делал проект в нише, которая Вам требуется - [что именно],
     прикрепил работы ниже:";
   - если совпадения нет, только смежное — аналог "Прикрепил пару своих
     последних работ в смежных нишах:".
2. Ссылки маскированными гиперссылками, каждая на отдельной строке, в формате
   [Работа 1](ССЫЛКА), [Работа 2](ССЫЛКА) — именно так, через markdown-формат
   квадратные+круглые скобки, без дополнительных слов или подписей кроме
   этого. Голые URL текстом подсознательно отпугивают — поэтому нужен именно
   такой маскированный формат, не "1. ссылка".
3. Блок про инструменты/навыки — суть из BRIEF, но сформулируй своими
   словами под контекст заказа — не копируй фразы примеров, не выдумывай
   новые навыки, которых нет в BRIEF.
4. Готовность работать + стимул скорости — буквально в духе "Если спишемся
   в ближайшее время, готов предоставить [результат] уже сегодня/завтра:)"
   — со смайликом ":)" в конце этого предложения, как в обоих примерах.
5. Фраза про опыт/надёжность — аналог "В этой нише я уже [срок], это моё
   основное направление" или "Уже не раз работал с крупными объёмами
   заказов" — бери из BRIEF, не выдумывай цифры.
6. Закрывающая строка буквально: "Надеюсь на Ваш ответ, с уважением,
   [Имя]!" (именно так, с восклицательным знаком, не просто имя отдельной
   строкой).

ОБЩИЕ ПРАВИЛА:
- Никаких "Также в моём портфолио есть..." и других вставок, которых нет
  в примерах — придерживайся ровно 6 блоков выше, не добавляй лишние абзацы,
  если их нет в скелете.
- "Вы" с большой буквы (если в твоей нише принято уважительное обращение),
  тон уверенный и дружелюбный, без формализма.
- Никаких абсолютных обещаний результата ("продажи взлетят в разы").
- Не звучи как рекламный буклет — без штампов копирайтера.
- Если совпадения совсем нет ни по теме, ни по смежной нише — следуй
  ПРАВИЛУ ВЫБОРА ПРИМЕРА из PORTFOLIO (пункт про "не вставляй пример").
"""

PROMPT_TEMPLATE = """Запрос #{nonce} — это отдельный, независимый запрос. Игнорируй любой
контекст, кроме того, что написано ниже в этом сообщении.

{brief}

{sales_principles}

{writing_rules}

{portfolio}

Вот ОДИН заказ (и только этот):
Заголовок: {title}
Бюджет: {budget}
Описание: {description}

Задача:
1. Оцени, действительно ли это реальная вакансия/заказ на монтаж видео
   (Reels/Shorts/TikTok, говорящая голова, сторителлинг, моушн-графика,
   нарезка вебинаров и т.п.), адресованная фрилансеру-монтажёру, а не обычная
   переписка/болтовня в чате и не пост монтажёра, который сам ищет заказ.
2. Если НЕ подходит — ответь ровно одним словом: NOT_RELEVANT
3. Если подходит — сначала выбери пример(ы) из PORTFOLIO по ПРАВИЛУ ВЫБОРА
   ПРИМЕРА, затем напиши отклик по СКЕЛЕТУ из WRITING_RULES, следуя порядку
   блоков буквально (на русском, без markdown, чистый текст с переносами строк).

ВАЖНО: ответ должен содержать ровно ОДИН черновик отклика для заказа выше.
Не упоминай, не пересказывай и не вставляй в ответ другие заказы, их заголовки
или ID — даже если они тебе кажутся знакомыми. Не используй никаких меток вида
"test1:", "===", заголовков-маркеров — только сам текст отклика.

Ответ дай ТОЛЬКО текстом отклика (или ровно NOT_RELEVANT), без пояснений.
"""


def ask_claude(prompt: str) -> str:
    # Запускаем из чистого временного каталога на каждый вызов: claude -p
    # подхватывает историю сессий, накопленную для текущей рабочей директории,
    # и без этого соседние заказы в одном прогоне могут "перемешаться" в ответах.
    with tempfile.TemporaryDirectory() as tmp_cwd:
        result = subprocess.run(
            [
                "claude",
                "-p",
                "--no-session-persistence",
                "--session-id",
                str(uuid.uuid4()),
                "--disallowedTools",
                "Read,Bash,Glob,Grep,WebFetch,WebSearch,Edit,Write,Task",
                "--",
                prompt,
            ],
            capture_output=True,
            text=True,
            encoding="utf-8",
            timeout=120,
            cwd=tmp_cwd,
        )
    if result.returncode != 0:
        raise RuntimeError(f"claude CLI failed: {result.stderr}")
    return result.stdout.strip()


def run(candidates: list[dict]) -> list[dict]:
    drafted = []
    for i, card in enumerate(candidates):
        if i > 0:
            # Без паузы быстрые подряд идущие вызовы claude -p иногда "слипаются" —
            # ответ на один заказ примешивает контекст соседних заказов из этого же
            # прогона. Эмпирически подтверждено: пауза снимает проблему полностью.
            time.sleep(5)

        prompt = PROMPT_TEMPLATE.format(
            nonce=uuid.uuid4(),
            brief=BRIEF,
            sales_principles=SALES_PRINCIPLES,
            writing_rules=WRITING_RULES,
            portfolio=PORTFOLIO,
            title=card["title"],
            budget=card.get("budget", ""),
            description=card["description"],
        )
        try:
            response = ask_claude(prompt)
        except Exception as e:
            print(f"Ошибка генерации для {card['id']}: {e}", file=sys.stderr)
            continue

        if response.strip() == "NOT_RELEVANT" or "NOT_RELEVANT" in response[:20]:
            continue

        drafted.append({**card, "draft": response})
    return drafted


if __name__ == "__main__":
    candidates = json.load(sys.stdin)
    result = run(candidates)
    json.dump(result, sys.stdout, ensure_ascii=False, indent=2)
